import { METHOD } from "./method.js";
import { PATH } from "./path.js";
import { STATUSCODE } from "./statusCode.js";
import { ROLE } from "./role.js";

export {
  METHOD,
  PATH,
  STATUSCODE,
  ROLE,
  // Add other constants here if needed
  // Example: ERROR_MESSAGES, SUCCESS_MESSAGES, etc.
  // You can create separate files for these constants as well
  // and import them here for better organization
};
